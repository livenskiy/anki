# Basic
Как в Python реализовать одиночное и множественное наследование?
Как проверить, является ли класс наследником другого класса?
Как из метода класса-наследника получить доступ к методам родителя (например, вызвать __init__ предка)?
Как получить имя собственного класса внутри метода так, чтобы оно корректно учитывало наследование (было именем класса экземпляра)?
Что такое MRO? Как программно посмотреть порядок разрешения методов для конкретного класса?
Что такое миксины (mixins), как они используются и как их принято называть?
Где физически хранятся атрибуты объекта и какое исключение выбрасывается, если атрибут не найден ни в одном классе цепочки?
Почему методы базового класса в Python видят атрибуты, определенные в производных классах (в отличие от статических языков вроде C++)?

# Advanced
Для чего используется метод __init_subclass__? В какой момент он вызывается и что получает в качестве аргументов?
Нужно ли что-то возвращать из метода __init_subclass__?
Как передать дополнительные именованные аргументы при определении класса (например, class MyClass(Base, version=1.0)) и как их перехватить?
Каким двум фундаментальным принципам (правилам) подчиняется построение MRO в Python?
Что произойдет, если иерархия классов построена так, что Python не может вычислить корректный и непротиворечивый MRO?
Как работает поиск через super() при множественном наследовании? Если class A(B, C, D), чей метод f() вызовет super().f() из класса A? А из класса B?

Попробуй вычислить MRO для класса Z:
python
class X: pass
class Y: pass
class A(X, Y): pass
class B(Y): pass
class Z(A, B): pass
Используйте код с осторожностью.

Как будет выглядеть цепочка обхода (MRO) для сложной иерархии, где A наследуется от B и C, B от D, а C от E?