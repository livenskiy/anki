# Basic
Что такое magic-методы и протоколы в Python? Приведи несколько примеров протоколов.
Какие методы входят в основную группу сравнения (comparison)? Какие из них Python может вывести автоматически?
Обязательно ли определять __ne__, если уже определен __eq__?
Как работает __eq__, определенный по умолчанию? Что он вернет для x = [1, 2, 3] и y = [1, 2, 3] при сравнении x == y?
Что нужно сделать, чтобы по объекту можно было итерироваться? Какой метод должен быть у самого итератора?
Как итератор сообщает Python, что итерирование закончено?
Какой magic-метод позволяет вызывать объект как функцию?
В каких конструкциях (операторах) используется метод проверки наличия элемента в контейнере?
Относятся ли операции сравнения, битовые сдвиги и функции вроде floor/trunc к группе числовых magic-методов?
Если для объекта определены только методы < и ==, будет ли автоматически работать оператор <=?

# Advanced
В какой именно момент жизненного цикла объекта вызывается метод __new__?
__len__ и __contains__. Когда если метода нет питон настырный и готов итерироваться хоть до конца контейнера а когда говорит "ну раз нет то нет"
Что означает префикс i в числовых методах (например, __iadd__)? Что происходит, если такой метод не определен в классе?
Что означает префикс r перед числовым методом (например, __radd__) и когда он вызывается?
Почему int успешно складывается с float, хотя метод int.__add__ «не знает», как работать с вещественными числами?
Какие методы входят в группу конвертации (conversion)? Входит ли туда строковое форматирование?
Вызываются ли magic-методы конвертации (вроде __int__ или __float__) неявно самим интерпретатором?
Зависят ли имена и количество параметров в методах группы container от того, является ли контейнер списком или словарем?
Как метод __len__ влияет на определение того, считается ли объект True или False в логическом контексте?
В чем принципиальная разница между __getattribute__ и __getattr__ при обращении к атрибутам?
Если мы хотим чтобы наш класс стал hashable что мы должны определить? Как проще всего получить хеш нашего объекта (с помощью какой стандартной функции?)