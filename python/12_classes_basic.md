# Basic
Что в Python не является объектом?
Какие плюсы возникают из-за того, что всё в языке является объектами?
Что такое identity объекта, как его получить и как его можно использовать?
Как получить тип объекта и как использовать этот тип для создания нового экземпляра?
Как надежно проверить, что объект относится к конкретному типу?
Чем принципиально отличается str(object) от repr(object)?
Что такое переменная класса, как задать её начальное значение и как изменить её внутри определения класса?
Как получить доступ к переменной класса извне? Чем этот процесс похож на обращение к переменной экземпляра (instance)?
Является ли ошибкой код: class Point: pass; p = Point(); p.color = 'red'?
Чем отличаются атрибуты, начинающиеся на _x, от атрибутов __y?
Что говорит пользователям класса название атрибута, начинающееся с одного подчеркивания _?
Есть ли в Python настоящая приватность данных? Как принято помечать данные как «внутренние»?
Как получить список всех методов и атрибутов объекта?
Как получить доступ к справочной информации (docstring) о классе прямо из программы?
Как получить объект класса, имея на руках только его экземпляр? Что при этом возвращается?

# Advanced
Всегда ли метод __del__ вызывается сразу после удаления последней ссылки на объект?
Что нужно делать, если объект открывает важные ресурсы, которые обязательно должны быть освобождены при удалении ссылки?
Какая встроенная функция возвращает именно атрибуты объекта вместе с их текущими значениями?
Существуют ли какие-либо ограничения на код, выполняемый внутри блока определения класса?
В чем кроется главная опасность (подвох) при попытке изменить переменную класса через экземпляр (self.var = ...)?
