# Basic
Тип символа: Существует ли в Python отдельный тип для одного символа (как char в C++)? Что возвращает индексация строки?
Изменяемость: Можно ли изменить символ в строке по индексу (например, s[0] = 'A')? Как получить измененную строку?
Сырые (raw) строки: Как задать строку, в которой обратные слэши не будут считаться спецсимволами (например, для путей Windows)?
Код символа: Как получить числовой код символа? Какой стандарт при этом используется — ASCII или Unicode?
Разворот: Как лаконично развернуть строку (reverse) с помощью среза?
Конвертация: Как преобразовать результат любого выражения к строковому представлению?
Форматирование (база): Спроси про способы форматирования и почему лучше пользоваться новыми чем старыми. Про новые спроси чем отличаются друг от друга и плюсы каждого варианта.
Проценты: Как вывести число 0.2 в формате 20%, используя возможности форматной строки?
Анатомия f-строки: Из каких основных частей состоит форматная спецификация внутри {} (порядок флагов, ширины, точности)? (подсказка - авария грузовика и автомобилей)
Неявная конвертация: Находятся ли str и bytes в отношениях автоматического приведения типов? Что нужно сделать, чтобы превратить одно в другое?

# Advanced
Бинарные данные: В чем принципиальное отличие bytes от bytearray? Какой тип элементов у байтовых строк в отличие от обычных?
Интерфейсы: Насколько сильно отличается набор методов (API) у bytes от str? Можно ли, например, искать подстроку в байтах?
Флаг !r: Попроси напечатать строку так чтобы нужно было использовать флаг !r 
Вложенность в f-строках: Можно ли использовать {} внутри других {}? Приведи пример, когда это нужно (например, динамическое заполнение или точность).
Функция format(): Чем отдельно стоящая функция format(value, spec) отличается от метода строк? В каких редких случаях она удобнее f-строк?
Динамические аргументы: Можно ли передавать параметры форматирования (например, ширину поля или количество знаков после запятой) как переменные?
Что умеет str.format чего не умеют f-строки? (не задавать вопрос напрямую а спросить как сделать такие вещи которые нельзя сделать через f-строки а можно через str.format например получение формата извне, передача словаря или использование позиционных аргументов)
Почему мы не можем сохранить f-строку в переменную и использовать её в качестве форматирующего выражения? (это похоже на предыдущий вопрос но важно поэтому я бы хотел чтобы он был задан)