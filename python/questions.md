# Prompt
я учу питон и прошёл книгу Python distilled. по итогам чтения я написал вопросы в формате анки по разделам, например классы или функции. я хочу использовать ИИ вместо анки. процесс такой - я присылаю тебе список вопросов из раздела и ты задаёшь их мне по очереди и оцениваешь то как я отвечаю . если я плаваю в теме можешь задать дополнительные вопросы, можно обсудить тему более глубоко. по итогам я прошу сформировать тебя небольшие заметки по слабым местам и тому на что обратить внимание с следующий раз. и дату когда будем повторять этот раздел в следущий раз.
Чтобы сложнее и интереснее было отвечать переформулируй вопросы и измени порядок но так чтобы вопросы по близким темам были рядом и сложность возрастала
Задавай вопросы по одному а не все сразу

# Python Anki Questions

### core
- Как задаются числовые литералы? Как интерператор понимает что это не имя а литерал?
- "Как добавить ""разряды"" к числовому литералу? Например у нас длинное бинарное число и мы хотим отделить разряды по 4?Это влияет на то как интерпретируется число?"
- "Как задать ""сырую"" строку в питоне? Например путь D:\folder\test.py"
- Допустим есть список и мы хотим в одну строку получить первый и последний элемент, как это можно сделать? numbers = [1, 2, 3, 4, 5] first, last = ?
- Почему это выражение не ошибка? Как Питон выполняет такое выражение? 3 < 5 > 4 Почему так сделано?
- Как проверить что water_temp от 20 до 40? (в одну строку и один раз использовал water temp)
- Как обменять значения без использования доп.переменной?
- Почему в питоне редко пишут циклы с индексами?
- Зачем нужны менеджеры контекста?
- "Обязательна ли часть ""as ..."" при работе с менеджером контекста?"
- Как написать аналог тернарного выражения в C++ (x < y ? x : y)
- Зачем нужен walrus operator :=?Почему нельзя было просто обойтись присваиванием?
- Для чего используется оператор is?если x и y одинаковые целые числа что возвращает x is y?если x и y одинаковые строки что возвращает?если x и y одинаковые списки?
- Какие два типа комментариев поддерживаются?
- Какие id у одинаковых строк и чисел?
- Чем лучше пользоваться вместо list1 = list2[:] ?Если нужно глубокое копирование может ли делать это slice? Как его сделать?
- Почему для проверки является ли что-то None рекомендуется писать value is None а не value == None?
- Чем отличается присваивание для изменяемых и неизменяемых (immutable) объектов? Что произойдёт в обоих случаях? x = 100; y = x; y += 1 x = [1, 2, 3]; y = x; y.pop()
- Типы для работы с бинарными данными в Питоне Чем bytes отличается от bytearray?

## data_structures
- Как получить длину списка, строки итд?
- Как проитерироваться в цикле по dictionary?Как получить ключи явно?Как проитерироваться по парам ключ-значение?Как получить только значения?
- Для каких стандартных контейнеров работает value unpacking? Должно ли количество элементов слева от присваивания быть равно кол-ву элементов контейнера?Как поступить если хочется записать значение в конкретные элементы и игнорировать остальные в конце?А если хочется игнорировать начало или середину?
- Гарантирован ли порядок элементов для словарей в питоне?Почему если да или если нет?
- Метод dict который возвращает значение ключа?Что он возвращает если ключа нет?Как передать ему дефолтное значение которое будет возвращаться если ключа нет?
- Как сделать dict comprehenision? Например у нас есть списки имён объектов и список значений и мы хотим сделать из них словарь
- Списки Должны ли элементы в списке быть одного типа?Что такое slice? Как трактуются значения до и после главного символа?Как получить последний элемент? Как получить предпоследний элемент?
- СпискиКак добавить значение в конец? В произвольную позицию?Как достать элемент по номеру и сохранить его значение?Как удалить интересующее значение? Что будет если значения нет?
- Как удалить элементы из контейнера, переменные, поля объекта итд?
- Списки Какая может быть проблема с этим кодом? colors = ['red', 'green', 'blue'] modified_colors=colors modified_colors.append('yellow')  Как исправить?
- Что такое slice? Какой формат?
- Что такое list comprehension? Какой формат?Привести пример как с его помощью можно получить список из квадратов чисел от 1 до 9 вкючительно
- Что такое range? Чем он лучше списка? Как получить список?
- Как проверить что элемент есть в списке?
- Допустим у нас есть список l = [1, 0, 5]. Как заменить его середину? например вставив [2, 3, 4] на место 0 так чтобы список стал [1, 2, 3, 4, 5]
- Чем отличается поведение среза [:] у списков когда он стоит слева и справа от операции присваивания?
- Чем отличается поведения срезов если они в левой или првой части присваивания?
- Что делает тип set? Чем он отличается от list и dict?Как задать set с элементами и без элементов?Какие требования к элементам?Обеспечивает ли set порядок элементов?
- Какие операторы естественные для множеств поддерживает set?Определено ли сложение (+)
- Как добавить элемент и несколько элементов в set?Как удалить элементы и чем remove отличается от discard?
- Чем tuple отличается от list? Как задать tuple?
- Как задать tuple из одного элемента?
- Что такое NamedTuple и как использовать?В каком модуле определена?Когда её удобнее использовать чем обычную и чем она удобнее класса?
- Могут ли кортежи содержат изменяемые объекты?

### str
- Как преобразовать результат выражения к строке?
- Перечислить способы форматирования строк в питонеКакими рекомендуется пользоваться а какими нет?
- Почему в методах __repr__ атрибуты обычно выводятся так? f'Account(self.owner!r)' Зачем нужно !r?
- Какие основные части форматной строки при форматировании строк?
- Можно ли в форматную строку передавать динамические аргументы?
- Как вывести проценты с помощью форматной строки? (например 0.2 как 20%)
- Типы элементов у обычных и байтовых строк
- Как получить код символа? Это будет ASCII код или юникод?
- Отличается ли интерфейс байтовой строки (bytes) от обычной (str)
- Назови максимальное кол-во форматирующих флагов в f-строке? (подсказка: авария грузовика и двух легковых автомобилей)
- Можно ли использовать вложенные {} в f-строка? Например символ заполнения тоже получать из переменной
- Есть ли в питоне отдельный тип символа?
- В каких отношениях бинарные типы вроде bytes и bytearray со строками? Конвертируются ли они в друг друга неявно?Что надо сделать чтобы отконвертировать одно в другое?
- как изменить содержимое строки в питоне?
- Что делает отдельно стоящая функция format?Какие у неё параметры?Какие причины использовать format вместо f-строк?
- Как развернуть строку с помощью среза?

### loop
- Зачем у for может быть else? Когда он выполняется?Если for пустой (тело никогда не выполнялось) вызывается ли else?Работает ли это с while?
- Где сюда поставить фильтрующее выражение? Например оставить только положительные числа positive_numbers = [x for x in numbers]
- Есть ли синтаксическая ошибка? [for x in numbers]
- Как в этом коде заменить отрицательные числа на 0? numbers = [-1, 2, -3, 4] [? for x in numbers]
- У нас есть список списков. Как сделать из него обычный список где все числа идут друг за другом? l = [ [1, 2, 3], [4, 5] ] [x ? for x in ?]
- Сколько if можно написать в списке? [? for x in items ?]
- Обязан ли у if в начале цикла for быть else?
- Если у нас в однострочном выражении несколько for сколько фильтрующих if можно поставить?
- "Мы хотим ""выпрямить"" список чтобы было [1, 2, 3, 4, 5] Код ниже падает с ошибкой "" 'int' object not iterable "" Что изменить в этом коде?  lists = [ [1, 2], 3, [4, 5] ]   [x  for g in lists  for x in g]"

## function
- как передать произвольное количество позиционных аргументов в функцию?
- Как происходит захват имён если функция создана в одном месте и используется в другом?  По копии или по ссылке?
- Если нужно чтобы функция принимала любые именованные аргументы как можно её определить?
- Как прописать у аргумента функции тип значения?Как прописать у возвращаемого функцией значения тип?Проверяет ли питон эти типы?
- Почему не рекомендуется использовать mutable в качестве дефолтных значений? Можно на примере этой функции:   def func(x, items=[]):     items.append(x)     return items   Как изменить функцию чтобы не было этой проблемы?
- Зачем может понадобиться написать функцию  так чтобы некоторые аргументы были только keyword? Как это сделать?
- Зачем может понадобиться определить функцию так чтобы некоторые аргументы передавались только как позиционные?Как это сделать?
- Что такое variadic аргументы? Какие два вида бывают и как задаются?Как можно развернуть набор variadic аргументов?
- Если функция должна вызывать получаемую функцию как аргумент и у неё есть параметры обязательно ли их делать парамерами этой функции? Как сделать без них?Если хочется прописать параметры так чтобы они могли быть любыми как это сделать?
- Как используется global для переменных внутри функции?
- Может ли функция использовать глобальные переменные?Можно и читать и писать?
- Как используется nonlocal?
- Что такое лямбда? Как её определить?Какие на неё накладываются ограничения?
- Если лямбда использует внешние переменные как захватываются значения? Значения запоминаются в момент создания лямбды или подставляются в момент вызова?
- Как работает capture внешних имён для функций питона? Происходит ли копирование значений и ссылок?
- Что можно сделать если нужно чтобы функция не подставляла значения захваченных переменных в момент вызова а сохранила их в момент захвата?
- Ограничено ли кол-во рекурсивных вызовов одной функции?
- Как при определении функции указать что она декорируется? Что будет происходить? В какой момент вызывается декоратор?Какой синтаксис у декоратора?
- Что делает питон если декоратор с параметрами (например @trace('message'))Как должен выглядеть декоратор с параметрами?
- Как в программировании называется ситуация когда у нас есть функция и набор значений и мы хотим вызвать функцию для каждого из элементов набора и сохранить результаты в список? Как обычно называется это действие?Какая функция сможет это сделать и какие у неё параметры?
- Как можно делать filter? (с list comprehensions и без)
- Что делает функция reduce?Какие у неё обязательные параметры? Как передать начальное значение в reduce? Что будет если его не передать?В каком модуле определён reduce?
- Как реализовать поведение аналогичное C++ static для функции? (так чтобы в функции были переменные запоминающие последние значения)
- Как определить async функцию? Чем async функция отличается от обычного с точки зрения своего запуска?Какие функции может вызывать async функция?Может ли вызывать асинхронные функции обычная функция?
- Как в анализе сигнатуры функции может помочь модуль inspect?
- Что означает символ / в определении функции? def f(x, y, /, *args, **kwargs):Как это используется?
- Может ли у лямбды не быть аргументов? Какой синтаксис такой лямбды?
- В чём проблема этой функции и как исправить? s = 'hello' def f():     print(s)     s = 'bye'
- Как лямбды захватывают внешние переменные? (те которых нет в списке аргументов) По значению или по ссылке?Если при определении лямбды у переменной одно значение а при вызове другое какое используется?Как захватить переменную по значению чтобы оно не изменялось при вызовах?
- Как используется partial?В каком модуле определена?Если мы используем аргументы в partial а потом вызываем функцию как отличается передача аргументов при вызове от лямбд?
- Что означает @имя написанное перед функцией?Как это использовать?
- Что это за странная конструкция? def func(x, y, /)
- Зачем в питоне positional-only аргументы? (подсказки - дизайн, изменения, kwargs)
- Если функция B создаётся внутри функции A и её объект сохраняется.  Что происходит при её вызове, что на видит из функции A?
- Изменится ли глобальная переменная? x = 42 def func():     x = 13
- Написать лямбду принимающую два аргумента и возвращающую сумму
- Написать лямбду возвращающую строковую константу
- Сколько выражений может быть в лямбде?
- Как то что функции захватывают внешний контекст используется в декораторах?
- LEGB правило
- Зачем нужны nonlocal и global внутри функции?

## exceptions
- Как выглядит блок обработки исключения?Как добавить блок который будет вызываться только если в try не было исключений?Как обработать несколько исключений?Как получит доступ к объекту исключения? например AttributeError
- Какие возможные части у блока обработки исключений?
- Как выбросить исключение которое приведёт к остановке программы?
- Может ли блок обработки исключений состоять только из try-else?
- Может ли быть блок обработки исключений с try-finally, без except?
- Какие есть варианты вызова raise?
- Когда исключения связываются?Как при обработке исключения понять что с ним связаны другие?
- Какой класс исключений предок всех остальных?Какой его потомок более полезен для программы?
- Почему в питоне считается плохой практикой писать except Exception?Почему практика указывать множество типов исключений вместо except Exception тоже не очень хорошая?
- Надо ли создавать новый except блок для каждого типа обрабатываемых исключений?
- Если надо гарантированно остановить программу с ошибкой что надо использовать?
- Что такое менеджеры контекста?У каких объектов обычно реализованы менеджеры контекста?Как добавить поддержку для собственных объектов?
- Как используется assert?Стоит ли использовать для проверки пользовательского ввода?Когда assert в программе не выполняются?
- Какие блоки будут вызываться в зависимости от того будет или нет выброшено исключение в блоке try?   try:     raise Exception() except:     print('except') else:     print('else') finally:     print('finally')
- Что происходит если во время обработки исключения выбрасывается ещё одно? Проблема ли это?
- Допустим мы хотим обработать исключение и заменить его на более высокоуровневое. Но при этом оставить информацию об оригинальном. Например мы импортируем файл и хотим выбросить высокоуровневую FileImportError но сохранить детали о том какая низкоуровневая ошибка привела к ошибке импорта.  Как это сделать?
- Почему если при обработке исключения надо выбросить ещё надо предпочитать raise ... from а не просто raise?


## iteration
- Что такое generator expression?Какие у него преимущества перед list comprehension?Сколько раз можно итерироваться по элементам?
- Как реализовать собственный генератор?
- Какие характеристики корутины есть у генератора?
- Как можно заменить deeply-nested код генераторами?Насколько это оптимально с точки зрения производительности? а по сравнению с возвращением списка?Почему принципы SOLID одобряют это решение?
- Что общего у генератора с итератором?
- Сколько раз можно пройти одну последовательность одним генератором?Как сделать так чтобы каждое итерирование начиналось с начала?
- Используются ли обычно значения которые возвращает генератор в return (не yield)?Как всё-таки получить значение если нужно?
- Где надо написать код деинициализации в генераторе который будет вызываться даже если генератор не выдал все элементы?Как ведёт себя context manager?
- Как передавать значения в генератор во время его работы?Как получить передаваемые извне значения в генераторе?
- "Где генератор будет останавливаться в этом коде?  def generator():     value = ''default""     while True:         value = yield len(value)         print(value)"
- Что такое generator comprehension? На что похоже по синтаксису?
- "Допустим генератор выделяет ресурсы и не полностью ""потреблён"". Пользователь остановил итерации досрочно. Как и где написать код который очистит ресурсы выделенные генератором?"
- Зачем используется yield from в генераторах?
- Что такое enhanced генераторы? Чем они отличаются от обычных?
- Как передать значение в enhanced генератор?Обработает ли генератор значение сразу или надо сделать что-то предварительно?
- Может ли enhanced генератор использовать yield и для возвращения и для получения значений?
- Как передать исключение в генератор?Где оно будет выброшено в генераторе?
- Отличаются ли generator.send(None) и generator.next() с точки зрения того как выполняется код генератора?Возвращает ли .send значения которые возвращает yield?
- Как остановить enhanced генератор?Какая его часть будет выполнена при остановке?
- У нас есть класс точки с 2 координатами и нам нужно сделать её итерируемой. Как это сделать?  p = Point(3, 5) coords = [c for c in p]
- У нас есть класс точки с произвольным кол-вом координат кототрые хранятся в списке.  Нужно сделать её итерируемой, как это сделать?  p = Point([3, 5, 6, 7]) coords = [c for c in p]
- Если мы хотим чтобы объект был итерируем и переопределили __iter__ но он возвращает себя и итерирование возможно только однократно насколько это нормально?
- Чем с точки зрения питона генераторы отличаются от итераторов?
- Какие у питона требования к объекту-итератору?
- Чем iterable объекты отличаются от итераторов?
- Может ли iterable объект быть итератором?
- Почему у всех итераторов должен быть определён метод __iter__?


### module
- Как импортировать произвольные функции или классы из модуля?В каком namespace оказывается импортированное?Как импортировать всё из модуля?Как импортировать с изменением имени?
- Какие пути видит Питон при импорте модулей? Как добавить новый путь?
- Как импортировать все имена из модуля?Почему это плохая практика?
- Почему сложно корректно обновить модуль в работающей программе?
- Что такое пакеты?Как реализовать вложенные пакеты?
- Зачем нужны файлы __init__.py внутри пакета?Когда они выполняются?
- Какую роль выполняет __main__.py внутри пакета?
- Чем __init__.py внутри пакета отличается от __main__.py?Может ли запускаться один без другого?
- Чем отличается запуск .py файла на исполнении внутри пакета по сравнению с обычным?
- Если один файл внутри пакета хочет импортировать другой как можно это сделать?Что если не хочется указывать полный путь к пакету?
- Какое количество точек можно использовать при импорте модулей из пакета?
- В каком порядке Python ищет модуль при выполнении import name?
- Сколько раз выполнится код модуля если импортировать его несколько раз?
- В чем разница между запуском python script.py и python -m package.script для относительных импортов?
- Зачем нужен файл __main__.py внутри папки пакета?
- Какое техническое преимущество дает наличие __init__.py в папке (кроме контроля экспорта)?
- Что означают две и три точки в from .. import и from ... import?
- Какое главное ограничение относительных импортов?
- Если мы создаём проект для разработки пакета какой вариант предпочесть а) Папка проекта это и есть папка пакета (__init__.py в корне) б) Папка проекта отдельно а в ней лежит пакет
- чем import smarthome.devices.light отличается от from smarthome.devices import light?
- Могут ли пакеты содержать другие пакеты?
- Обязателен ли __init__.py для того чтобы пакет был пакетом?
- Если мы хотим сделать в программе папки с файлами .py нужно делать пакет?
- Если мы хотим сделать для нашего пакета фасад (внести какие-то важные импорты и спрятать остальные) как это сделать?
- Почему для проекта с такой структурой 'python project.py' не сработает? [project]     project.py (import config.config.py)     [config]         log.py         config.py (import log.py)
- "Объясни фразу ""пакеты круче потому что у модулей в них есть пакето-сознание. а у модулей не из пакета syspath-орукость"""
- Работают ли точки с import path (import ..path, import . another)

### class
### object
- Как получить у объекта все его методы?
- Есть ли приватные данные у объектов?Как пометить данные объекта как внутренние?
- Что не является объектом в питоне?
- Что такое identity объекта и как его получить?Как может использоваться identity объекта?
- Как получить тип объекта?Как можно использовать тип для создания объекта?
- Как проверить что объект относится к типу?
- Чем отличается str(object) от repr(object)?
- Какие плюсы возникают из-за того что всё является объектами?
- Как определить деструктор у объекта?

## basics_and_attributes
- Чем отличаются атрибуты _x и __y?
- Как получить доступ к справочной информации о классах из программы на питоне?
- Что такое классовая переменная?Как удобнее всего задать её начальное значение?Если мы хотим изменить её внутри класса как выглядит синтаксис?Как получить доступ к ней извне класса (пользователю класса)?Чем доступ извне похож на instance переменную?
- Является ли это ошибкой? class Point: pass p = Point(); p.color = 'red'
- Какая функция возвращает атрибуты объекта со значениями?
- Как получить класс объекта по объекту?Что возвращается?
- Есть ли в питоне приватность?Как можно её реализовать?
- Что говорит пользователям класса названия атрибутов начинающися с _
- Какие три типа properties существуют?
- Как определить классовый декоратор?Отличаются ли они от декораторов для функций?
- Можно ли определить сеттер и делеттер поля без геттера?
- Есть ли какие-то ограничения на код выполняемый внутри определения класса?
- Можно ли создавать классы без использования ключевого слова class?
- Чем технически отличается Data Descriptor от Non-Data Descriptor?
- В каком порядке Python ищет атрибут obj.x, если в классе есть Data Descriptor, а в самом объекте obj.__dict__['x'] тоже есть значение?
- Зачем в __get__(self, instance, owner) нужен параметр owner (или cls)?
- Какой тип у всех классов?
- Можно ли создать класс не используя ключевое слово class?
- "Как передать произвольные параметры (например, version=""1.0"") из объявления класса class MyPlugin(Base, version=""1.0""): в механизм инициализации?"
- Какие варианты есть если хочется вписаться в логику создания классов?Чем один лучше другого?
- Как ещё можно создавать в питоне классы кроме как написал в коде class MyClass?

## methods_and_properties
- Есть ли в питоне виртуальные методы?
- Методы получения атрибута объекта по имени, установки, проверки существует ли и удаления
- Чем классовые методы отличаются от статических?
- Как используются property? Как их задать для геттеров и сеттеров?Почему если использовать имена переменных внутри properties будет рекурсия? как её избежать?
- Как сделать абстрактный базовый класс в питоне?Что будет происходить если по ошибке будет создан объект такого класса или потомок переопределит не все абстрактные методы?
- Если взять метод объекта и сохранить как вызвать его для объекта? p = Point() f = p.print() f(???)
- Что такое классовые методы?Как задать классовый метод? Какие особенности аргументов метода?Как он вызывается?
- Что такое статические методы?Как задать такой метод?Как использовать?
- Чем классовые методы ближе к классу чем статические?
- Чем классовые методы в питоне отличаются от статических в языках вроде C++?
- Что будет если определить для поля @property но не определить @field.setter?
- Что будет если определить для поля @field.setter но не определить @property?
- Обязательно ли для полей классов определять property?
- Можно ли определять property не соответствующей никакой переменной класса?
- Зачем используется метод __init_subclass__?Когда он вызывается?Что он получает? Надо ли что-то возвращать?
- Чем __new__ отличается от __init__?Может ли вызываться один из методов?Когда __new__ вызывается а __init__ нет? Что для этого нужно?
- Что такое дескрипторы?Почему их методы get, set, delete получают instance?
- В каком порядке в объекте ищутся атрибуты и почему? обычные атрибуты (obj.x)методы (obj.func())property и дескрипторыклассовые атрибуты (Class.x) Какая роль в этой схеме у базовых классов?
- Что будет найдено раньше, атрибут объекта или property самого базового из его классов?
- Как реализованы instance-методы в питоне?
- Почему мы можем переписать метод объекта, просто присвоив ему значение (obj.method = 10), хотя метод определен в классе?
- Как надо манипулировать атрибутами в методах __getattribute__ и __setattr__ чтобы не было рекурсии?
- Если нужно создать собственный метакласс как это сделать? (подсказка - определяемые методы есть и у объектов кроме одного)

## inheritance_and_mro
- Как в методах наследника получить доступ к методам предка? (например вызывать конструктор родительского класса из дочернего)
- Как получить имя собственного класса из метода с учётом того что может быть наследование?
- Как унаследовать класс от другого?А от нескольких?
- Как проверить является ли переменная наследником класса?
- Как получить внутри класса-наследника ссылку на его базовый класс?
- super и множественное наследование. У нас есть класс class A(B, C, D) Допустим все классы имеют одинаковый метод f().  Метод какого класса вызывается при вызове super().f() из A, B, C?
- Что такое MRO? (наследование)Как можно изменять MRO при создании класса с множественным наследованием?Как можно посмотреть на него для класса?
- Что такое миксины и как они используются?Как рекомендуют называть эти классы?
- Почему при наследовании если атрибут определён и в базовом классе и производных методы базового класса работают с атрибутом из производного? (не как в C++ где методы базового класса видят только атрибуты базового класса)
- Где находятся атрибуты объекта?Как выполняется полный поиск атрибутов объекта (с учётом наследования)?Какое исключение выбрасывается если атрибут не найден?
- Как будет работать обход базовых классов через super() для иерархии классов A -> B, C    B -> D     C-> E где A последний наследник?
- Каков MRO для класса Z?  class X: pass class Y: pass class A(X, Y): pass class B(Y): pass class Z(A, B): pass
- Каким двум простым принципам подчиняется построение MRO в Python?
- Что происходит если  иерархия определена так что Питон не может корректно построить MRO?
- В питоне при задании цепочки наследования иногда передают и аргументы. Если нужно сделать это у своих классов как?  class MyClass(Base, version=1.0)

## advanced_and_metaprogramming
- Всегда ли __del__ для объекта вызывается сразу после удаления последней ссылки на него?Что нужно делать если объект открывает какие-то важные ресурсы и они обязательно должны быть освобождены при удалении ссылки на объект?
- Почему наборы аргументов передаваемых в __new__ и __init__ обычно совпадают?
- Как создать слабую ссылку в питоне? (weakref)Синтаксис использования
- Что такое метаклассы?Зачем может понадобиться создавать собственный метакласс?Какой самый часто использующийся метакласс?
- Почему __getattribute__ это охранник клуба а __getattr__ бюро находок?
- Чем отличаются data от nondata дескрипторы?
- С какой целью у data дескрипторов приоритет выше чем у атрибутов?
- Почему опасно хранить данные экземпляров прямо в атрибуте дескриптора (например, в self.value)?
- Что такое метакласс и как он соотносится с обычными классами и объектами?
- В какой момент вступает в дело метакласс (через __new__), в отличие от __init_subclass__?
- Зачем вообще нужно переопределять __new__ у классов когда есть __init__? (хотя бы одно применение)


### comprehension
- Бывает ли tuple comprehension?
- Какие есть виды comprehensions? Чем отличается их синтаксис?

### file
- Как открыть файл и прочитать или записать всё его содержимое?Какие режимы открытия файлов существуют и чем отличаются?Как открыть файл так чтобы не надо было закрывать его вручную?
- Как итерироваться по строкам файла?Как прочитать все строки файла как список?Будут ли обрезаны переводы строк в результатах чтения файла?
- Как записать в json данные из программы?Как прочитать из json данные?Какие типы данных можно сохранять?
- Как открыть файл для чтения-записи?Один ли это режим или два разных?
- Как открыть файл для записи но только в том случае если его нет. Если файл есть получить ошибку?
- Чем интерфейс файла открытого в бинарном режиме отличается от текстового?
- Записывают ли функции .write и .writelines возвраты строки?
- Как указать кодироку и обработку ошибок при работе с файлом?
- Как получить стандартный ввод, вывод и поток ошибок?
- Функция получения списка файлов в директории
- Аргумент функции print для печати в файл а не stdout
- Какие методы os позволяют безопасно перезаписать файл? (модифицировать его не изменяя пока он не будет заполнен)
- Как получить доступ к переменным окружения?Можно ли их изменять?

### jupyter_notebook
- Какие преимущества у jupyter notebook перед интерактивным терминалом?Что такое ячейки? Видят ли ячейки результаты друг друга?Как запустить jupyter notebook?Чем lab отличается от notebook?

### library
- Какую функцию можно вызвать в программе чтобы она остановилась и был вызван отладчик?Какой отладчик вызывается по-умолчанию?
- Как использовать print так чтобы после того как строка выведена не было перевода строки?
- Как используется enumerate?  Почему лучше использовать его чем писать for i in range(...)?
- Как используется zip?Какое количество последовательностей он может принимать?Что будет если одна меньше остальных?
- Как в Nix создать исполняемый скрипт на питоне?
- Для чего используется matplotlib? Как с помощью неё построить простой график?Почему вывод на экран если запустить программу из терминала или vscode может не работать?Как вывести результаты в файл?
- функция для получения пользовательского ввода в консоличто надо будет сделать если хочется получить число?

### protocols
- Что такое magic методы и протоколы?Какие протоколы можно назвать?
- Когда вызывается magic метод __new__?
- Зачем у числовых magic-методов определены методы с i?Что означает i?Что происходит если они не определены?
- Что означает r перед названием числового magic-метода, например __radd__?
- Почему int складывается с float несмотря на то что int.__add__ не умеет складываться с float числами?
- Что такое magic методы?Какие основные группы можно назвать? (o**t, n**s, c**n, c**n, c**r, i**n, a**s, f**n, c**t m**r)
- Основные методы группы comparisionКакие методы из группы выводятся питоном автоматически?Обязательно ли определять ne если определено eq?
- Как работает __eq__ определённый по умолчанию?Что даст для этих объектов? (x = [1, 2, 3]; y = [1, 2, 3]; x == y)
- Основные методы группы conversionВходит ли строковое форматирование в эту группу? Какой метод?
- Вызываются ли magic-методы конвертирования неявно?
- Основные magic методы группы containerЗависит ли количество параметров у методов и их имена от того является контейнер спископодобным или ассоциативным?Как len помогает определять True объект или False?В каких конструкциях используется метод проверки наличия элемента?
- Что делают magic методы группы attribute?Чем __getattribute __отличается от __getattr__?
- magic-метод который позволяет запускать объект как функцию
- Если мы хотим чтобы по нашему объекту можно было итерироваться что надо сделать?Какой magic метод должен быть у итератора?Как определяется что итерирование законечно?
- Если определён метод для < и метод для == будет ли работать <=?
- Примеры числовых magic-методовОтносятся ли к ним сравнения?Относятся ли сдвиги?Относятся ли операции вроде floor, trunc?

### unittest
- Стандартный модуль для юнит-тестов? Как создать собственные тесты? Функции для тестирования условий
- Как проверить в тестах что выбрасывается исключение?
- Как делать что-то перед каждым тестом и после каждого теста?
- Как запустить юнит-тест в файле программы?Как запустить тесты в папке?Как запустить произвольный файл/test_case/метод?Что делает -v?
- Как вызвать минимальный юниттест в том же файле что и код?

### venv
- Зачем нужно виртуальное окружение?Как создать виртуальное окружение для своей программы?Как запустить и выйти из него?Как сделать так чтобы vscode работала из виртуального окружения?

### advanced
- Как с помощью exec создать функцию в программе? Куда она попадёт? (подсказка - помни про namespace)
- Что делает функция exec? Зачем она принимает globals и locals? Что будет если их не передавать?Может ли она изменить глобальные и локальные переменные?
- Почему КАТЕГОРИЧЕСКИ НЕЛЬЗЯ передавать в exec и eval внешние данные?