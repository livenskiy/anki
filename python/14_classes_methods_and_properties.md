# Basic
Есть ли в Python виртуальные методы в том понимании, как в C++ или Java?
Какие встроенные функции используются для получения, установки, проверки существования и удаления атрибута объекта по его имени (строке)?
Что такое классовые методы (@classmethod)? Как их задать, каковы особенности их аргументов и как они вызываются?
Что такое статические методы (@staticmethod)? Как их определить и в каких случаях использовать?
В чем главное различие между классовыми и статическими методами?
Как используются property? Как с их помощью задать геттеры и сеттеры для атрибута?
Обязательно ли для всех полей класса определять property?
Можно ли определить property, которое не связано ни с какой реальной переменной (атрибутом) класса?
Что произойдет, если определить для поля @property (геттер), но не определить соответствующий @field.setter?
Можно ли определить сеттер или делеттер для поля, если у него отсутствует геттер?
Если сохранить метод экземпляра в переменную (f = p.method), как его потом вызвать? Нужно ли передавать self вручную?
Как создать абстрактный базовый класс в Python?
Что произойдет, если попытаться создать экземпляр абстрактного класса или если его потомок не переопределит все абстрактные методы?

# Advanced
Почему при использовании имен переменных внутри properties часто возникает рекурсия и как её избежать?
Чем классовые методы в Python концептуально ближе к классу, чем статические?
В чем отличие классовых методов Python от статических методов в языках вроде C++?
В чем фундаментальное различие между __new__ и __init__?
В какой ситуации __new__ будет вызван, а __init__ — нет? Что для этого должно произойти?
Почему __getattribute__ называют «охранником клуба», а __getattr__ — «бюро находок»?
В каком строгом порядке Python ищет атрибуты (обычные поля, методы, property, классовые переменные)? Какую роль здесь играют базовые классы?
Что будет найдено раньше: обычный атрибут экземпляра или property, определенное в самом базовом (родительском) классе?
Почему мы можем «затереть» метод объекта, просто присвоив ему значение (obj.method = 10), хотя сам метод определен в коде класса?
Как правильно манипулировать атрибутами внутри __getattribute__ и __setattr__, чтобы избежать бесконечной рекурсии? Спроси в этом вопросе почему super().__getattribute__ лучше чем обращение к словарю
