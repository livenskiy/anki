# Basic
Чем отличается iterable объект от итератора? Приведите примеры каждого.
Может ли iterable объект быть одновременно итератором? Если да, то при каком условии?
Почему у всех итераторов должен быть определён метод __iter__, даже если он возвращает self?
Что такое list comprehension? Напишите формат и пример: получить список квадратов чисел от 1 до 9 включительно.
Синтаксис Comprehensions: Является ли выражение [for x in numbers] валидным? Чего в нем не хватает?
Какие виды comprehensions существуют в Python? Чем отличается их синтаксис (list, set, dict)?
Существует ли tuple comprehension? Почему (x for x in range(5)) создаёт не кортеж?
Что такое generator expression? Чем он отличается от list comprehension по синтаксису и поведению?
next(): Дай простую задачу где надо использовать next() (например читать файл по строкам и пропустить первую)
Задачка которая потребует использовать itertools.chain
Задачка которая потребует использовать itertools.count
Задачка которая потребует использовать itertools.cycle
Вопрос про то является ли range генератором и его возможности (индексация, поддержка len, проверка вхождения in за константное время)

Создание итераторов в классах
У нас есть класс Point с двумя координатами. Как сделать его итерируемым, чтобы работало:
p = Point(3, 5); coords = [c for c in p]?

У нас есть класс Point с произвольным количеством координат в списке. Как сделать его итерируемым?
p = Point([3, 5, 6, 7]); coords = [c for c in p]

# Advanced
Какие у Python требования к объекту-итератору (какие методы нужно реализовать)?
Если мы переопределили __iter__, но он возвращает self, и итерирование возможно только однократно — насколько это нормально? Когда такое может понадобиться?
