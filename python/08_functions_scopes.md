# Basic
Правило LEGB: Расшифруй аббревиатуру LEGB. В каком порядке Python ищет имя переменной?
Чтение vs Запись (Global): Может ли функция читать значение глобальной переменной без специальных ключевых слов? А что произойдет, если попытаться изменить её значение внутри функции (например, x = 13)?
Ключевое слово global: Как явно указать функции, что присваивание должно изменить переменную в глобальной области видимости?
Ключевое слово nonlocal: Для чего оно нужно и чем отличается от global? С какой областью видимости оно работает?
Ошибка UnboundLocalError: В чем проблема кода ниже? Почему Python падает на print(s), хотя глобальная s существует?
python
s = 'hello'
def f():
    print(s)
    s = 'bye'
Используйте код с осторожностью.

# Advanced
Захват имен (Capture): Когда функция захватывает внешнюю переменную, она делает «копию» значения или сохраняет «ссылку» на имя в контексте? Что произойдет, если внешняя переменная изменится до того, как функция будет вызвана?
Ловушка позднего связывания (Late Binding): Если лямбда создается в цикле и использует переменную цикла, какое значение она «увидит» при вызове? Почему в таких случаях пишут x=x в списке аргументов?
Замыкания и состояние: Если функция B создается внутри функции A и возвращается наружу, что происходит с локальными переменными функции A? Как это свойство используется в декораторах?
Функции высшего порядка: Если функция должна вызвать другую функцию с произвольными аргументами, как это реализовать максимально гибко через *args и **kwargs?
Модуль functools.partial: Как работает partial? В чем его ключевое отличие от использования лямбды в контексте передачи и «замораживания» аргументов?
Эмуляция контекста: Если нам нужно, чтобы функция «запомнила» значение переменной именно в момент своего создания (а не в момент вызова), какие есть способы это сделать (помимо x=x)?