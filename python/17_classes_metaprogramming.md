# Basic
Что такое метаклассы? Как они соотносятся с обычными классами и объектами?
Какой тип (type) у всех классов в Python по умолчанию? Какой метакласс является самым часто используемым?
Можно ли создавать классы динамически без использования ключевого слова class? Если да, то как?
Зачем вообще нужно переопределять метод __new__ у классов, если есть __init__? Приведи хотя бы один пример применения.
Что делает функция exec()? Зачем ей передаются словари globals и locals?
Почему категорически нельзя передавать во встроенные функции exec() и eval() внешние (непроверенные) данные?

# Advanced
Если нужно создать собственный метакласс, от чего он должен наследоваться и какие методы обычно переопределяются?
Как передать произвольные параметры (например, version="1.0") из заголовка class MyPlugin(Base, version="1.0"): в механизм создания класса?
Какие существуют варианты «вписаться» в логику создания классов (например, __init_subclass__ vs метаклассы)? В каких случаях один способ предпочтительнее другого?
В чем разница в моменте срабатывания метакласса (через его __new__) и метода __init_subclass__ базового класса?
Почему наборы аргументов, передаваемых в __new__ и __init__ метакласса, обычно полностью совпадают?
Как с помощью exec() динамически создать функцию в программе и как контролировать, в какой неймспейс (пространство имен) она попадет?
Может ли функция exec() изменить локальные переменные в текущей области видимости? Что произойдет, если не передать словари контекста?