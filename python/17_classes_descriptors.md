# Basic
Что такое дескрипторы и какую задачу они решают в дизайне языка?
Какие методы входят в протокол дескриптора и почему они обязательно получают параметр instance?
В чем техническое различие между Data Descriptor и Non-Data Descriptor?
Как в Python реализованы обычные методы экземпляра (instance methods) с точки зрения протокола дескрипторов?
Зачем в сигнатуре метода __get__(self, instance, owner) нужен параметр owner (или cls)?

# Advanced
В каком строгом порядке Python ищет атрибут obj.x, если имя x одновременно есть и в Data Descriptor класса, и в локальном словаре объекта obj.__dict__?
С какой целью Data дескрипторам сознательно дан более высокий приоритет поиска, чем обычным атрибутам в __dict__ экземпляра?
В чем заключается главная опасность хранения данных экземпляров прямо в атрибуте самого дескриптора (например, в self.value)? Как это отразится на разных объектах одного класса?
Что произойдет, если в дескрипторе реализован только __get__, и мы попробуем присвоить значение obj.x = 10? Как изменится поведение поиска после этого присваивания?
Как используется __set_name__ в дескрипторах (не используй __set_name__ в вопросе а приведи пример с дескриптором старого стиля где имя передаётся в конструкторе и спроси как сделать лучше)
Почему в методах дескриптора используется слово owner а не cls? 