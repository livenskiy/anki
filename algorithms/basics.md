# Основное
Что за нотация "большое О"?
Почему принято говорить сложность сортировки вставкой O(n) а не O(n/2) хотя количество элементов всё время сокращается
Что такое задача коммивояжёра? Какая её сложность с точки зрения O(n)?

# Рекурсия
В чём заключается метод "разделяй и властвуй" применительно к решению задач?
Почему при решении задач с рекурсией важно начать с самого простого случая (например сортировки массива из 0, 1, 2 элементов)

# Сортировки
Верно ли утверждение "массивы более эффективны чем списки потому что у них быстрый произвольный доступ поэтому сортировки можно делать за logN а у списков это всегда квадрат"?
Как устроена merge sort? 
Почему merge sort редко применяется для сортировки массивов?
Как устроена быстрая сортировка? Всегда ли она даёт NlogN (описать худший случай)
Верно ли утверждение что быстрая сортировка требует random access и поэтому не работает для списков?
Правда ли что в языках используется базовый quick sort? Если нет в чём отличия алгоритмов от quick sort (без подробностей, хочется услышать ответ про то что используются оптимизации)

# SkipList
В чём преимущества Skip List? Какая скорость доступа и как за это приходится платить памятью? 
Почему часто используется в больших базах данных?
Описать устройсто списка. Рассказать про то как реализована вставка и поиск (без подробностей, смысл)

# Хеш-таблицы
Описать на пальцах как устроена самая простая хеш-таблица (можно в рамках задачи - у тебя нет хеш-таблицы, надо сделать простую).
Спросить про коллизии и критическая ли это проблема.
Спросить как реализована хеш-таблица в С++ (без особых деталей, просто нужно проверить что я помню про бакеты)
Спросить пару хитрых вопросов на понимание load factor и как мы можем выбирать хеш-таблица быстрее но использует больше памяти или экономичнее но с коллизиями